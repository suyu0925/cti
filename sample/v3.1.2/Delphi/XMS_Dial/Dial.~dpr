program Dial;

uses
  ShareMem,
  Forms,
  DJDial in 'DJDial.pas' {Form1},
  GlobalVal in 'GlobalVal.pas',
  XMS_Dial_Event in 'XMS_Dial_Event.pas',
  XMS_Dial_Sub in 'XMS_Dial_Sub.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
